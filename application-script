
:) read zero -> start after the fourth line and get the number of lines (when a letter is reached, return)
#read valmax length -> unnecessary
:) convert int to string
:) convert string to int
#create two empty matrices 0.0 -> $a0 and $a1 = x and y : one for int and other for char (maxval)
#read one -> store the size of the file (after the fourth line)
#read two -> read, after the fourth line, the file and write it on a string
#in the end, lw the number of files, divide the matrix per the n of files
#in the end, will have only one open and close file
